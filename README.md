# casan.se

Generated using Go Hugo and hosted on GitHub Pages.

For development, use this:

```sh
hugo server --buildDrafts --buildFuture
```

Currently using Hugo version `v0.140.0`.

## TODO
- Fix code font for mobile (include font in website)
- Fix Tags pages
